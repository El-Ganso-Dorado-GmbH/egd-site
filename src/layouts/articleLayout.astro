---
import Layout from "./generalLayout.astro";

const {title, description, author, pubDate, topRibbon, 
	botRibbon = {
	txt: "Interested in learning more?",
	buttonTxt: "Articles",
	buttonForm: "/articles",
	},
} = Astro.props.frontmatter;
---
<Layout title={title} topRibbonVars={topRibbon} botRibbonVars={botRibbon}>
	<slot />
</Layout>
