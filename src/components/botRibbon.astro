---
const {txt, buttonTxt, buttonType="buttonDarkGrey", buttonForm="/contact"} = Astro.props.vars;
---

<div class="botRibbon">
	<span class="ribbon"></span>
	<h3 set:html={txt}>
		Tell us about your goals, and we'll help you build a development roadmap.
	</h3>
	<form action={buttonForm}>
		<button class={buttonType} set:html={buttonTxt} />
	</form>
</div>

<style>
	.botRibbon{
		display: flex;
		height: auto;
		min-height: 200px;
		width: 100%;
		justify-content: center;
		align-items: center;
		flex-direction: column;
		text-align: center;
		background-color: var(--egdOrange);
	}.botRibbon h3{
		margin: 20px 10%;
		color: var(--egdLightGrey);
	}.botRibbon button{
		margin: 10px auto;
	}
</style>
