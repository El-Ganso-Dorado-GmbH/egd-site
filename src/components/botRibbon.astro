---
const {txt, buttonTxt, buttonType="buttonOrange", buttonForm="/contact"} = Astro.props.vars;
---

<div class="botRibbon">
	<span class="ribbon"></span>
	<h3 set:html={txt}>
		Tell us about your goals, and we'll help you build a development roadmap.
	</h3>
	<form action={buttonForm}>
		<button class={buttonType} set:html={buttonTxt} />
	</form>
</div>

<style>
	.botRibbon{
		height: auto;
		width: 100%;
		text-align: center;
		background-color: var(--egdOrange);
	}.botRibbon h3{
		margin: 20px 10%;
		color: var(--egdLightGrey);
	}.botRibbon button{
		margin: 10px auto;
	}
</style>
